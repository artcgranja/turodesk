POSTGRES_HOST=127.0.0.1
POSTGRES_PORT=5544
POSTGRES_USER=turodesk
POSTGRES_PASSWORD=turodesk
POSTGRES_DB=turodesk

# Redis
REDIS_URI=redis://langgraph-redis:6379

# Postgres para checkpointer e filas
# Ajuste user/password/database conforme docker-compose
DATABASE_URI=postgres://turodesk:turodesk@langgraph-postgres:5432/turodesk?sslmode=disable
# Banco de memória (separado)
MEM_DATABASE_URI=postgres://turodesk:turodesk@langgraph-mem-postgres:5432/turodesk_mem?sslmode=disable

# OpenAI
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4o-mini

# LangSmith (opcional)
LANGSMITH_TRACING="true"
LANGSMITH_ENDPOINT="https://api.smith.langchain.com"
LANGSMITH_API_KEY="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"
LANGSMITH_PROJECT="turodesk"

# Ajuste o nome da imagem se usar langgraph build
IMAGE_NAME=arthurgranja/turodesk-langgraph-api:0.1.0

# Settings sugeridos no AI Context
SHORT_TERM_RETENTION_DAYS=30
LONG_TERM_IMPORTANCE_THRESHOLD=0.5
EMBEDDING_DIMENSION=1536