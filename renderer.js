// Sem funcionalidades por enquanto. Mantemos apenas pequenos aprimoramentos de UX.
window.addEventListener('DOMContentLoaded', () => {
  const input = document.querySelector('input');
  if (input) {
    input.focus();
  }
});


